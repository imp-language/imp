



export func padStart(self string, targetLength int, padString string) string {
    val num = targetLength - length(padString)
    mut start = ""
    for val i = 0; i < num; i++ {
        start = start + padString
    }

    return start + self
}