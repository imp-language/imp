// Declares variables of primitive types and changes
// their types with addition or reassignment.

log("declarations")

mut i = 0
log(i)

mut f = 0.0
log(f)

mut b = false
log(b)

mut s = "string"
log(s)

log("")
log("operations")

i = i + 1
log(i)

f = f + 1.0
log(f)

b = true
log(b)

s = s + " variable" + " a"
log(s)

log("\nnegatives")
mut neg_i = -1
log(neg_i)
neg_i = neg_i + -23
log(neg_i)

mut neg_f = -1.0
log(neg_f)
neg_f++
log(neg_f)

mut neg_d = -1.0d
log(neg_d)
neg_d = neg_d + 346.0d
log(neg_d)
