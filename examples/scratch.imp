

import "util/strings"

import "io"
import "net"



io.write("a.txt", "data")


val a = read("reee")

log(a)

log(4)

write("e", "v")

voidFunc("ree")


func write(s string, d string) {
    log("reeeeeee")
}

func voidFunc(s string) {
    log("from void")
}

func read2(s string) string {
    log("hello, not modules")
    // todo: error when fail to return
    return "a" + s
}