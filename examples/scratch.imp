

val a = [0, 1, 2, 3]
val s = ["reee", "beee", "veee", "yeee"]

func takesListOfInt(listOfInt int[]) {
    log("list")
    log(listOfInt)
}

takesListOfInt(a)
takesListOfInt(s) // todo: this should error, it doesn't
takesListOfInts(4)